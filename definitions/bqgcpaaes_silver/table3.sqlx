-- models/tables/minha_tabela.sqlx

config {
  type: "operation",     -- executa o script exatamente como está
  tags: ["tabelas_base"]
}

create table if not exists `$(dataform.projectConfig.vars.data_product).dataform.minha_tabela` (
  id int64,
  nome string,
  data timestamp
)
partition by date(data)
cluster by id;
